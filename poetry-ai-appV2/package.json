{
  "name": "poetry-ai-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next export",
    "start": "next start",
    "postinstall": "next telemetry disable",
    "netlify-build": "npm install && npm run build && npm run export"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.8",
    "next": "^14.1.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@netlify/plugin-nextjs": "^4.39.5",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
